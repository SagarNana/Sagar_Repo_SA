package com.abc.Utility;

import java.time.Duration;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class WaitUtility {

	/*//WaitUtility we to mentain all method wait so create class waitutility
	
	public static void WaitForPageLoad(WebElement element) {
		
		WebDriverWait webdriverwait=new WebDriverWait(BrowserUtility.driver, Duration.ofSeconds(5));
	// this is way right way to proper 10 second otherwise thread.sleep but don not use thread.sleep
		
		webdriverwait.until(ExpectedConditions.elementToBeClickable(element));
	}*/
	
	
}
